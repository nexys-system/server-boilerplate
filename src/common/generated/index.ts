// this is an empty model. It needs to be updated with the actual model
// run `yarn getAssets` to generate
import { Entity } from '@nexys/core/dist/query/model/type';

const model: Entity[] = [];

export default model;
